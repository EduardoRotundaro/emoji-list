import React, {useEffect, useState} from 'react';
import PropTypes from 'prop-types';

function Footer({}) {
    useEffect(() => {
    }, []);

    return (
        <footer>
        </footer>
    );
}

Footer.propTypes = {
};

export default Footer;
