import React, {useEffect, useState} from 'react';
import PropTypes from 'prop-types';

function Header({}) {
    useEffect(() => {
    }, []);

    return (
        <header>
        </header>
    );
}

Header.propTypes = {
};

export default Header;
