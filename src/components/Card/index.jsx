import React, {useEffect, useState} from 'react';
import PropTypes from 'prop-types';

function Card({}) {
    useEffect(() => {
    }, []);

    return (
        <div className="">
        </div>
    );
}

Card.propTypes = {
};

export default Card;
