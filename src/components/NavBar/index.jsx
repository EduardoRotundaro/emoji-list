import React, {useEffect, useState} from 'react';
import PropTypes from 'prop-types';

function NavBar({}) {
    useEffect(() => {
    }, []);

    return (
        <nav>
        </nav>
    );
}

NavBar.propTypes = {
};

export default NavBar;
