import React, {useEffect, useState} from 'react';
import PropTypes from 'prop-types';

function Activity({}) {
    useEffect(() => {
    }, []);

    return (
        <section className="">
        </section>
    );
}

Activity.propTypes = {
};

export default Activity;
