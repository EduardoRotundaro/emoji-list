import React, {useEffect, useState} from 'react';
import PropTypes from 'prop-types';

function FoodDrink({}) {
    useEffect(() => {
    }, []);

    return (
        <section className="">
        </section>
    );
}

FoodDrink.propTypes = {
};

export default FoodDrink;
