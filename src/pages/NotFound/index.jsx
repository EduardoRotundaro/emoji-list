import React, {useEffect, useState} from 'react';
import PropTypes from 'prop-types';

function NotFound({}) {
    useEffect(() => {
    }, []);

    return (
        <section className="">
        </section>
    );
}

NotFound.propTypes = {
};

export default NotFound;
