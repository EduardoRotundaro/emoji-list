import React, {useEffect} from 'react';

function Flags() {
    useEffect(() => {
    }, []);

    return (
        <section className="">
            flags
        </section>
    );
}

export default Flags;
