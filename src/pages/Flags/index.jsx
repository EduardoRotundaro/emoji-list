import React, {useEffect, useState} from 'react';
import PropTypes from 'prop-types';

function Flags({}) {
    useEffect(() => {
    }, []);

    return (
        <section className="">
        </section>
    );
}

Flags.propTypes = {
};

export default Flags;
