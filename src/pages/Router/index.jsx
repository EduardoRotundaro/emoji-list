import React, {useEffect, useState} from 'react';
import PropTypes from 'prop-types';

function Router({}) {
    useEffect(() => {
    }, []);

    return (
        <section className="">
        </section>
    );
}

Router.propTypes = {
};

export default Router;
