import React, {useEffect, useState} from 'react';
import PropTypes from 'prop-types';

function Objects({}) {
    useEffect(() => {
    }, []);

    return (
        <section className="">
        </section>
    );
}

Objects.propTypes = {
};

export default Objects;
