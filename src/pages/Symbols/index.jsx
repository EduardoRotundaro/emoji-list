import React, {useEffect} from 'react';

function Symbols() {
    useEffect(() => {
    }, []);

    return (
        <section className="">
            symbols
        </section>
    );
}

export default Symbols;
