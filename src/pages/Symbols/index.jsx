import React, {useEffect, useState} from 'react';
import PropTypes from 'prop-types';

function Symbols({}) {
    useEffect(() => {
    }, []);

    return (
        <section className="">
        </section>
    );
}

Symbols.propTypes = {
};

export default Symbols;
